# mi-lista-enlaces

## 🚀 Ejecución con Docker

Para garantizar que el entorno de auditoría sea idéntico, se recomienda el uso de Docker.

### 1. Construcción y Arranque
Ejecuta el siguiente comando en la raíz del proyecto para descargar dependencias, construir la imagen y levantar el servidor:

```bash
docker compose up --build